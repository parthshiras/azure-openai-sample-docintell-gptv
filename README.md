# Azure OpenAI Sample with Document Intelligence and GPT Vision
Image analysis with GPT Vision and Doc Intelligence

# NOTE

This branch is for comparing two documents.  It also adds the infra bicep so you don't have to deploy everything by hand